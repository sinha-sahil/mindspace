# Phase 2 · Setup

## Objective

Create the new directory skeleton without yet moving code.

## Tasks

- [ ] `mkdir src/lib/client/components`
- [ ] `mkdir -p src/lib/client/modules/{workspaces,projects,passkeys,theme,sidebar,whiteboard,auth}/ui`
- [ ] Each module gets an empty `index.ts`, `store.ts` (where needed), `types.ts`, `remote.ts` (where applicable)

## Outputs

Empty target directories ready for the move phase.

## Validation

`tree src/lib/client` shows the layout.
